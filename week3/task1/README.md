# Task 1 - SQL Benchmark Dataset (Ground Truth)

`SQL_query_explanation.docx` already contains, for all 50 benchmark
questions: the question, the ground-truth SQL, the result (row count /
sample), and a short explanation of how each query works. 

`seed.sql` + `docker-compose.yml` spin up the same `classicmodels`
Postgres database used by Task 2/3/4.
